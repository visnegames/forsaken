const root=document.documentElement;addEventListener('pointermove',e=>{root.style.setProperty('--mx',e.clientX+'px');root.style.setProperty('--my',e.clientY+'px')});
const audio=document.querySelector('#ambience'),volume=document.querySelector('#volume'),vol=document.querySelector('#vol'),mute=document.querySelector('#mute');audio.volume=.22;let started=false;const startAudio=()=>{if(started)return;audio.play().then(()=>started=true).catch(()=>{})};addEventListener('pointerdown',startAudio,{once:true});addEventListener('keydown',startAudio,{once:true});volume.addEventListener('input',()=>{audio.volume=volume.value/100;audio.muted=false;mute.textContent='♫';vol.textContent=volume.value+'%';startAudio()});mute.addEventListener('click',()=>{startAudio();audio.muted=!audio.muted;mute.textContent=audio.muted?'×':'♫'});
const obs=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('in')),{threshold:.14});document.querySelectorAll('.reveal').forEach(x=>obs.observe(x));
const bg=document.querySelector('[data-parallax]');addEventListener('pointermove',e=>{if(innerWidth<800)return;let x=(e.clientX/innerWidth-.5)*12,y=(e.clientY/innerHeight-.5)*8;bg.style.transform=`scale(1.07) translate(${x}px,${y}px)`});
const sparks=document.querySelector('#sparks');for(let i=0;i<30;i++){let s=document.createElement('i');s.className='spark';s.style.left=Math.random()*100+'vw';s.style.animationDuration=(8+Math.random()*15)+'s';s.style.animationDelay=(-Math.random()*15)+'s';s.style.opacity=Math.random()*.7;sparks.appendChild(s)}
document.querySelectorAll('.magnetic').forEach(b=>{b.addEventListener('pointermove',e=>{let r=b.getBoundingClientRect(),x=(e.clientX-r.left-r.width/2)*.08,y=(e.clientY-r.top-r.height/2)*.08;b.style.transform=`translate(${x}px,${y}px)`});b.addEventListener('pointerleave',()=>b.style.transform='')});
